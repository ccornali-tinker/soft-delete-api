services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: items_interview
    ports:
      - "5432:5432"
    volumes:
      - items_pg_data:/var/lib/postgresql/data

volumes:
  items_pg_data:
